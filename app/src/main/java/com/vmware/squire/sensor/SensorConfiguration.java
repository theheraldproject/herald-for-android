package com.vmware.squire.sensor;

import com.vmware.squire.R;

/// Global configuration for sensor.
public class SensorConfiguration {
    // Define icon for notification message that is mandatory for enabling background operation with a foreground service.
    public final static int notificationIconResource = R.drawable.virus;
}
